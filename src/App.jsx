import{Routes,Route,Navigate}from'react-router-dom';import{AuthProvider,useAuth}from'./context/Auth';import Landing from'./pages/Landing';import{Login,Register}from'./pages/Auth';import Onboarding from'./pages/Onboarding';import Shell from'./components/Shell';import*as S from'./pages/Student';import*as P from'./pages/ParentAdmin';
function Guard({roles,children}){const{user}=useAuth();if(!user)return <Navigate to="/login"/>;if(roles&&!roles.includes(user.role))return <Navigate to="/"/>;return <Shell>{children}</Shell>}
export default function App(){return <AuthProvider><Routes><Route path="/" element={<Landing/>}/><Route path="/login" element={<Login/>}/><Route path="/register" element={<Register/>}/><Route path="/onboarding" element={<Guard roles={['student']}><Onboarding/></Guard>}/>{[['/student/dashboard',S.Dashboard],['/student/ai',S.AI],['/student/careers',S.Careers],['/student/careers/:id',S.CareerDetail],['/student/roadmaps/:id',S.Roadmap],['/student/scholarships',S.Scholarships],['/student/scholarships/:id',S.ScholarshipDetail],['/student/saved',S.Saved],['/student/deadlines',S.Deadlines],['/student/study',S.Study],['/student/resources',S.Resources],['/student/notifications',S.Notifications],['/student/profile',S.Profile],['/student/settings',S.Settings]].map(([p,C])=><Route key={p} path={p} element={<Guard roles={['student']}><C/></Guard>}/>)}{[['/parent/dashboard',P.ParentDashboard],['/parent/careers',P.ParentCareers],['/parent/ai',P.ParentAI],['/parent/resources',P.ParentResources],['/parent/shared-progress',P.Shared]].map(([p,C])=><Route key={p} path={p} element={<Guard roles={['parent']}><C/></Guard>}/>)}{[['/admin/dashboard',P.AdminDashboard],['/admin/users',P.AdminUsers],['/admin/careers',P.AdminCareers],['/admin/scholarships',P.AdminScholarships],['/admin/resources',P.AdminResources]].map(([p,C])=><Route key={p} path={p} element={<Guard roles={['admin']}><C/></Guard>}/>)}<Route path="*" element={<Navigate to="/"/>}/></Routes></AuthProvider>}
