import{useEffect,useRef,useState}from'react';import{aiService}from'../services/services';import{get,set}from'../utils/storage';import{Button}from'./UI';export default function Chat(){const[m,setM]=useState(get('chat',[]));const[q,setQ]=useState('');const[load,setLoad]=useState(false);const end=useRef();useEffect(()=>{set('chat',m);end.current?.scrollIntoView({behavior:'smooth'})},[m]);const send=async x=>{x=(x||q).trim();if(!x||load)return;setQ('');setM(a=>[...a,{r:'u',t:x}]);setLoad(true);const a=await aiService(x);setM(v=>[...v,{r:'a',t:a}]);setLoad(false)};return <div className="chat"><div className="chat-head"><b>✦ EduGuide AI</b><small>Frontend AI simulation</small></div><div className="messages">{!m.length&&<div className="welcome"><div>✦</div><h2>Hello 👋</h2><p>Ask about careers, scholarships, study plans, AI/ML or placements.</p><div className="prompts">{['Which career suits me?','Find scholarships for me','Create a study plan','How can I start AI/ML?','How do I prepare for placements?'].map(x=><button onClick={()=>send(x)} key={x}>{x}</button>)}</div></div>}{m.map((x,i)=><div className={'msg '+x.r} key={i}>{x.t}</div>)}{load&&<div className="msg a">Typing…</div>}<div ref={end}/></div><div className="chat-input"><textarea value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}}} placeholder="Ask anything..."/><Button onClick={()=>send()}>Send</Button></div></div>}
