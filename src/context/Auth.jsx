import{createContext,useContext,useState}from'react';import{auth}from'../services/services';const C=createContext();export const AuthProvider=({children})=>{const[user,setUser]=useState(auth.current());return <C.Provider value={{user,login:(e,p)=>{const u=auth.login(e,p);setUser(u);return u},register:d=>{const u=auth.register(d);setUser({id:u.id,name:u.name,email:u.email,role:u.role});return u},logout:()=>{auth.logout();setUser(null)}}}>{children}</C.Provider>};export const useAuth=()=>useContext(C);
